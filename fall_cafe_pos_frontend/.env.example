# Frontend environment variables for Autumn Brew POS
# Copy to .env and fill in values. If left empty, the app will use local fallbacks and may not persist data.

REACT_APP_SUPABASE_URL=
REACT_APP_SUPABASE_KEY=
# Optional, used for auth email redirects if enabling auth flows
REACT_APP_SITE_URL=http://localhost:3000
